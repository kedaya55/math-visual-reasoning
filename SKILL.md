---
name: math-visual-reasoning
description: 将数学知识点制作成简洁、连贯的推理动画视频，用可追踪的图形、数量和参数变化解释为什么成立。适用于数学教学动画、几何证明可视化、公式直观推导、函数变化讲解及此类视频修改。仅解题、静态课件、普通科普视频或现成视频内容分析不默认使用。
---

# 数学推理动画

让学生通过数学对象的连续变化看见“为什么”，文字负责点题、必要条件与结论。对图形能表达的关键推理，先设计使关系显现的动作，再写配合观察的短句；不要先写完整讲稿，再给每段配图。上一段的结果应成为下一段的起点。

## 四条核心原则

1. **依据可追踪**：提前呈现的是已知条件、合法构造及已建立的关系，不是尚待证明的结论；题目、猜想或结果预告可先出现，但须明确身份，不能当作依据。转化开始前，用必要的标注或视觉标记明确下一步推理所需依据的来源；转化中随对象保持对应，不能在结束后才补出关键标记。必要边长、条件或数值也要保持归属；依据已被明确建立的新表达承接后，才可退出。
2. **变化有数学理由**：操作对应真实关系，公式项有可见来源。区分例子、直觉、近似与一般证明；仅在条件满足时声称不变量成立。动画应呈现关系的形成、条件的变化或不变量的保持。若仅将静态图表、文字或公式依次显示，未帮助解释“为什么”，应重新选择表征或动作。
3. **共享一个状态**：图形、标注和数值由同一模型与时间状态更新。保留对象身份；任意时间定位可重现，不能依赖逐帧累积。
4. **一次看清一个关系**：初态可读，运动可追踪，落定后有比较时间。对应关系难以辨认时，用有明确含义的视觉分组和分步动作降低追踪难度。文字按当前步骤需要出现；已由图形或新表达承接的说明及时退出，避免标题、正文与字幕重复解释同一关系。简洁不能删掉必要条件，连续不等于不停运动。

数学标注的颜色绑定明确含义，在推理链中保持一致，且不作为唯一识别依据；字幕默认统一白色。项目视觉说明集中定义配色与标题、公式、字幕字体，具体映射见连续动画参考。

默认采用左侧信息、右侧推理动画，主要讲解段不交换左右职责；不自动添加栏目名、装饰页眉页脚或进度条。左侧只点明当前问题、必要条件或结论；底部教学字幕在讲解阶段持续保留，随动作提供简短观察提示、必要条件或收束；推理的主要证据由动画呈现，不能用字幕补救缺失的关键变化。避免左侧正文与字幕逐句重复，观察停顿时保留当前字幕。用户明确指定其他设计时覆盖这些默认值；区域分工与比较画面的处理见连续动画参考。

## 输入、范围与底座

- 必需输入是一个知识点或明确问题。缺少受众时按常见初学阶段说明假设后继续；只有歧义会改变核心论证时才澄清。输入过宽时先提出一个具体可教的问题。
- 可覆盖的制作默认值：中文、1920×1080、60 fps、约 60–120 秒、深色背景、无配音和音乐。时长服务于理解，不记为用户长期偏好。
- 按用户请求交付方案、核心片段、局部修改或完整视频；不擅自扩充范围，不把内部制作顺序变成审批停点，已授权的本地导出不重复询问。
- 实际视频工作先读 `hyperframes` 入口，写动画读 `hyperframes-core`、`hyperframes-animation`，执行框架命令读 `hyperframes-cli`。已有工程沿用原状态；需要声音才读 `media-use`，不自建渲染引擎。
- 按当前 skill 目录定位依赖，不写死用户路径、不猜命令；必需依赖缺失时明确阻塞。不默认启动子代理，也不为数学图形额外采购素材。

## 工作流程

### 1. 明确论证和依据

读 [数学推理设计](references/reasoning-design.md)。在工程 `STORYBOARD.md` 记录“为什么”问题、先修知识、变量与条件、关键误解、表征和论证链。表征应适合题目，不把所有主题套进面积拼图。

为关键推理在已有分镜记录“当前已知 → 操作及理由 → 新得到的关系”，并将依据对应填入下表；不另建重复文档。图形、路线、辅助线与高亮也会传达结论，不能只检查公式的出现顺序。制作时可预先求解；教学时按依据是否成立安排显示，详见推理设计参考。

| 下一步结论 | 所需可见依据 | 绑定的对象或属性 | 何时可由新表达承接 |
| --- | --- | --- | --- |
| 例如：矩形面积是 ab | 两条相邻边分别为 a、b | 该矩形的两条物理边 | 两条边与乘积的对应已建立，ab 留在该矩形上 |

### 2. 设计并实现关键变化

读 [连续动画制作](references/continuous-animation.md)。在已有分镜中先写清“哪个对象 → 做什么动作 → 保持或改变什么关系 → 观众因此看出什么”，再补配套短句；与第一步论证链合并记录，不另建动作表。标明初态、注意焦点、终态、下一步承接与观察时间；将连续对象与视觉要求写入工作流已有的 `BRIEF.md` 和视觉说明。

- 先实现最能暴露理解难点的核心段，再完成所请求的范围；已有工程只改相关部分。
- 用可计算图形表达数学关系，图形和标注共用模型，不能让生成式视频猜测几何与公式。
- 小学、初中题目先按随包[课程知识点索引](references/curriculum-coverage.md)选表征，再查[组件入口](references/components.md)，按需读取覆盖表与接口；按组件入口的复用层级先检查完整组件，再按具体能力缺口使用模型或底层工具；基础形体与默认视觉沿用既有案例，教学动作按题目设计。只把本题所需文件复制到工程。组件负责表征，不能替代本题推理和动作设计。
- 立体展开、截面或表面路线按需读[空间几何约定](references/spatial-geometry.md)，先建立真实几何与材料对应，再决定投影和取景。
- 出现随刚体移动的边长标注时，按需读 [边长标注组件](references/edge-label.md)，复用附带组件；无此需求不加载。面积模型可按需读 [完全平方案例](references/complete-square-example.md)；设计面积重排、对应消去与区域到公式的接续时，读 [面积重排动作案例](references/area-rearrangement-example.md)。
- 有配音时以实际音频时间安排动作；无声版按阅读、运动与观察安排，不等待音频。

### 3. 检查并交付

读 [验收方法](references/verification.md)。分镜进入动画实现前先做推理顺序检查，成片检查时再核对实际出现时机；不新增用户审批停点。关键片段遮住解释性文字，保留必要数学标注，检查动作能否呈现所声称的关系；断点优先改表征与动作。先查本题数学关系，再查框架与实际画面；关键动作查看起点、中途、终点和衔接两侧，并逐项追踪上述依据。局部修复只复查受影响部分。

请求视频时交付可播放 MP4、可编辑工程和实际检查结果；只请求方案或修改时交付对应成果。工程保留工作流已有的 `BRIEF.md`、`STORYBOARD.md`、视觉说明和 `QA.md`，有旁白才保留旁白与音频时间资料。成果按当前输出约定保存，不把某次视频的数据写回 skill。

区分组件测试、工程检查、成片检查和学生学习效果。未完成的检查如实记录；依赖故障重复且无新进展时报告具体阻塞，不盲试或宣称通过。
