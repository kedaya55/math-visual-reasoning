# 组件展示页检查

本页保留原有组件的历史检查结果，不代表当前全部能力。v0.2 新增范围与检查见 [补充组件 QA](EXPANSION-QA.md)，知识点与当前边界见 [课程索引](CURRICULUM-COVERAGE.md)。

2026-09-07。

## 坐标轴

- `axes-preview.html` 离线可用。检查 x 范围 ±2/±3/±4、y 范围 ±1/±2/±3、间距 45/55 的 18 组组合：数字无重叠或裁切，原点只显示一次，更新后的正负坐标映射正确。
- 既有第一象限默认参数、非法参数拒绝并保留原状态、恢复按钮、键盘和 390 px 窄屏检查通过，无浏览器运行错误。已有 3 项坐标计算测试通过。
- 已查看默认、最小、最大及手机截图，证据位于 `work/axes-preview/`。新增范围与间距调整尚未接入视频时间线。

## 矩形尺寸

- `rectangle-preview.html` 离线可用。9 组底、高组合检查通过，矩形与标注原地更新，标注在对应边中点外侧，图形和文字均在画布内。
- 调整尺寸后，四条边在 0°/45°/90° 下与矩形实际变换后的端点一致；原旋转中心保留。非法尺寸被拒绝并保留原尺寸。
- 恢复按钮、键盘调整、390 px 窄屏检查通过，无浏览器运行错误。已查看默认、宽扁、窄高及手机截图，证据位于 `work/rectangle-preview/`。
- 验证范围为尺寸交互及几何绑定，新增尺寸变化尚未接入视频时间线。

## 圆形

- `circle-preview.html` 离线可用，无浏览器运行错误。检查半径 1.0/2.2/3.6（每单位 50 px），半径线长度等于圆半径，端点位于圆周，r 标注跟随线段中点，图形与标注均在画布内。
- 恢复按钮、键盘调整、非法参数拒绝及原状态保留通过；已查看最小、默认、最大半径和 390 px 窄屏截图，无横向溢出。
- 证据位于 `work/circle-preview/`，默认预览为 `circle-preview.png`。验证范围为交互预览，尚未接入视频时间线。

## 直角三角形

- `triangle-preview.html` 离线可用，无浏览器运行错误。检查底 2/5.4/7.2、高 2/3.9/5.4 的 9 组组合，轮廓周长与几何长度一致，底、高标注跟随边中点，六个文字标注均在画布内。
- 恢复按钮与键盘调整通过；非法尺寸和坐标被拒绝，并保留原图形。
- 已查看默认、宽扁、窄高及 390 px 窄屏截图；页面无横向溢出。证据位于 `work/triangle-preview/`，默认预览为 `triangle-preview.png`。
- 验证范围为直角三角形交互预览，尚未接入视频时间线或进行用户验收。

## 矩形与斜率

- `showcase.html` 可直接以本地文件打开，无网络依赖。浏览器运行错误 0。
- 实际操作 0°、45°、90° 按钮及尺寸、角度、倍率、位置控件。三组尺寸/倍率/位置 × 九个角度，共 27 组：始终只有一个矩形和两个边长标签，标签正立且位于画布内。
- 斜率演示使用同一数学点 `(Δx, k·Δx)` 更新点位置、横纵线段、Δx/Δy 标注、坐标值和比值。检查 k=0.25/0.5/1 与 Δx=1/4/6 的 9 组组合，坐标和显示比值正确。
- 已查看矩形 0°/45°/90°、斜率默认与最大参数截图。将点坐标移到右侧说明、图中仅保留 P，并放在点的左上方，避免直线穿过坐标文字。
- 已查看 720 px 宽窗口截图；页面无横向溢出，控制区堆叠到图下方。
- 坐标映射、非原点增量、正负/水平斜率、垂直线和非法输入共 3 项测试通过。
- 证据位于 `work/component-showcase/`。这些是组件和交互演示的检查，不代表斜率视频或学生学习效果已验证。

复用发现：边长组件可同时用于刚体矩形和随参数变化的横纵增量；复用的是几何绑定。矩形的固定尺寸与坐标例子的变化端点需要不同的数学状态，不能统一成一个只会平移/旋转的“万能图形”。
